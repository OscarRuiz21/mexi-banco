# Modelo de dominio

El estado vigente del dominio, las decisiones y el glosario está en `docs/contexto/CONTEXT.md` (generado; no se edita). Las decisiones de arquitectura formales están en `docs/adr/`. Si el proyecto tiene un plan o glosario propio, enlázalo aquí:

- (por ejemplo `docs/plan/...`, `docs/glosario.md`)
