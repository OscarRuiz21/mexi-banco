# Personas que pueden aceptar decisiones

Cuentas de GitHub de personas (no de agentes). Solo un merge hecho por una de ellas puede dejar una entrada de `decisiones` o `arquitectura` en `estado: aceptada`; el job `compuerta` de `contexto.yml` deja `main` en rojo si no.

- @OscarRuiz21
