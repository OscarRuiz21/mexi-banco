# Contrato del proyecto con la fábrica

slug: mexi-banco
modo: experimento
idioma: español
tracker: GitHub Issues (OscarRuiz21/mexi-banco)
contexto: docs/contexto (entradas por PR; CONTEXT.md generado en main)
adr: docs/adr (si existe)
specs: docs/specs (si existe; en modo riguroso, si no hay spec, el issue con brief completo hace de spec)
skills-locales: .agents/skills

## Qué significa el modo

Ver `~/fabrica/procesos/modos.md`. Cambiar el modo es un PR a este archivo con una línea de justificación.

## Adaptadores locales

Skills que solo aplican aquí van en `.agents/skills/<nombre>/SKILL.md`. Si una skill sirve a más de un proyecto, va a la fábrica por PR.
